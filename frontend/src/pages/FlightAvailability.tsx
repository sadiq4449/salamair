import FlightDemoSchedule from './FlightDemoSchedule';

/** Demo parity: mock schedule from `flightMock.ts` (see `FlightDemoSchedule`). */
export default function FlightAvailability() {
  return <FlightDemoSchedule />;
}
